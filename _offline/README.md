# Scripts Offline

Scripts para popular o site com dados do Meetup. Execute `npm install` para instalar dependências e `npm run scrape-events` para buscar eventos passados do grupo BitDevsSSA no Meetup.
